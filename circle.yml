machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - echo $PWD
    - ls
    - docker build -t circleci/dmm-web -f ./Dockerfile

test:
  override:
    - docker run -d -p 6080:80 circleci/dmm-web; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:6080
